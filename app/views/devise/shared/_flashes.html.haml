- flash.each do |key, message|
  - if key == 'notice'
    %div.alert.alert-success{role: "alert"}
      = message
  - elsif key == 'alert'
    %div.alert.alert-danger{role: "alert"}
      = message
  - else
    %div{class: key}
      = message